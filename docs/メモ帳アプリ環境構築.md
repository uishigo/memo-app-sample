# 📝 メモ帳アプリ 環境構築手順

React（フロントエンド）+ Express（バックエンド）+ Supabase（データベース）を使ったメモ帳アプリの構築手順です。

---

> ⚠️ **この端末での注意事項（社内ネットワーク環境）**
>
> 社内プロキシが HTTPS 通信を SSL インスペクション（中間者検査）するため、Node.js から Supabase への接続が `TypeError: fetch failed` で失敗します。
> Node.js は Windows の証明書ストアではなく独自のルート証明書を使うため、社内プロキシの証明書を信頼できないことが原因です。
>
> **対処：** `server/index.js` の先頭に以下を追加してください。
>
> ```js
> process.env.NODE_TLS_REJECT_UNAUTHORIZED = "0";
> ```
>
> または `server/.env` に以下を追加し、サーバー起動前に読み込まれるようにしてください。
>
> ```
> NODE_TLS_REJECT_UNAUTHORIZED=0
> ```
>
> **注意：** これは開発環境限定の対処です。本番環境では使用しないでください。
> 正式な対処は IT 部門から社内 CA 証明書を取得し、`NODE_EXTRA_CA_CERTS` 環境変数に設定することです。

---

## 📁 ディレクトリ構成

```
memo-app/
├── client/          # React フロントエンド
└── server/          # Express バックエンド
```

---

## 🔧 前提条件

以下がインストールされていることを確認してください。

- **Node.js** v18以上 → https://nodejs.org/
- **npm** v9以上（Node.jsに同梱）
- **Git** → https://git-scm.com/

バージョン確認：

```bash
node -v
npm -v
git --version
```

---

## 1️⃣ プロジェクトの作成

```bash
mkdir memo-app
cd memo-app
```

---

## 2️⃣ バックエンド（Express）のセットアップ

```bash
mkdir server
cd server
npm init -y
npm install express cors dotenv @supabase/supabase-js
npm install --save-dev nodemon
```

### `server/index.js` を作成

```js
const express = require("express");
const cors = require("cors");
require("dotenv").config();

const app = express();
const PORT = process.env.PORT || 3001;

app.use(cors());
app.use(express.json());

// Supabase クライアントの初期化
const { createClient } = require("@supabase/supabase-js");
const supabase = createClient(
  process.env.SUPABASE_URL,
  process.env.SUPABASE_KEY,
);

// メモ一覧の取得
app.get("/api/memos", async (req, res) => {
  const { data, error } = await supabase
    .from("memos")
    .select("*")
    .order("created_at", { ascending: false });
  if (error) return res.status(500).json({ error: error.message });
  res.json(data);
});

// メモの作成
app.post("/api/memos", async (req, res) => {
  const { title, content } = req.body;
  const { data, error } = await supabase
    .from("memos")
    .insert([{ title, content }])
    .select();
  if (error) return res.status(500).json({ error: error.message });
  res.status(201).json(data[0]);
});

// メモの更新
app.put("/api/memos/:id", async (req, res) => {
  const { id } = req.params;
  const { title, content } = req.body;
  const { data, error } = await supabase
    .from("memos")
    .update({ title, content })
    .eq("id", id)
    .select();
  if (error) return res.status(500).json({ error: error.message });
  res.json(data[0]);
});

// メモの削除
app.delete("/api/memos/:id", async (req, res) => {
  const { id } = req.params;
  const { error } = await supabase.from("memos").delete().eq("id", id);
  if (error) return res.status(500).json({ error: error.message });
  res.status(204).send();
});

app.listen(PORT, () => {
  console.log(`Server running on http://localhost:${PORT}`);
});
```

### `server/package.json` のスクリプトを編集

```json
"scripts": {
  "start": "node index.js",
  "dev": "nodemon index.js"
}
```

### `server/.env` を作成（後でSupabaseの値を入力）

```env
SUPABASE_URL=your_supabase_url
SUPABASE_KEY=your_supabase_anon_key
PORT=3001
```

> ⚠️ `.env` はGitにコミットしないでください。

### `server/.gitignore` を作成

```
node_modules/
.env
```

---

## 3️⃣ フロントエンド（React）のセットアップ

`memo-app/` ルートに戻って：

```bash
cd ..
npm create vite@latest client -- --template react
cd client
npm install
npm install axios
```

### `client/src/App.tsx` を作成

```jsx
import { useState, useEffect } from 'react';
import axios from 'axios';

const API = 'http://localhost:3001/api';

interface Memo {
  id: number;
  title: string;
  content: string;
  created_at: string;
}

function App() {
  const [memos, setMemos] = useState<Memo[]>([]);
  const [title, setTitle] = useState('');
  const [content, setContent] = useState('');
  const [editingId, setEditingId] = useState<number | null>(null);

  useEffect(() => {
    axios.get<Memo[]>(`${API}/memos`).then(res => setMemos(res.data));
  }, []);

  const fetchMemos = async () => {
    const res = await axios.get<Memo[]>(`${API}/memos`);
    setMemos(res.data);
  };

  const handleSubmit = async () => {
    if (!title.trim()) return;
    if (editingId) {
      await axios.put(`${API}/memos/${editingId}`, { title, content });
      setEditingId(null);
    } else {
      await axios.post(`${API}/memos`, { title, content });
    }
    setTitle('');
    setContent('');
    fetchMemos();
  };

  const handleEdit = (memo: Memo) => {
    setEditingId(memo.id);
    setTitle(memo.title);
    setContent(memo.content);
  };

  const handleDelete = async (id: number) => {
    await axios.delete(`${API}/memos/${id}`);
    fetchMemos();
  };

  return (
    <div style={{ maxWidth: 600, margin: '40px auto', fontFamily: 'sans-serif' }}>
      <h1>📝 メモ帳</h1>

      <div style={{ marginBottom: 24 }}>
        <input
          placeholder="タイトル"
          value={title}
          onChange={e => setTitle(e.target.value)}
          style={{ display: 'block', width: '100%', marginBottom: 8, padding: 8 }}
        />
        <textarea
          placeholder="内容"
          value={content}
          onChange={e => setContent(e.target.value)}
          rows={4}
          style={{ display: 'block', width: '100%', marginBottom: 8, padding: 8 }}
        />
        <button onClick={handleSubmit}>
          {editingId ? '更新' : '追加'}
        </button>
        {editingId && (
          <button onClick={() => { setEditingId(null); setTitle(''); setContent(''); }} style={{ marginLeft: 8 }}>
            キャンセル
          </button>
        )}
      </div>

      {memos.map(memo => (
        <div key={memo.id} style={{ border: '1px solid #ccc', borderRadius: 8, padding: 16, marginBottom: 12 }}>
          <h3 style={{ margin: '0 0 8px' }}>{memo.title}</h3>
          <p style={{ margin: '0 0 12px', whiteSpace: 'pre-wrap' }}>{memo.content}</p>
          <button onClick={() => handleEdit(memo)} style={{ marginRight: 8 }}>編集</button>
          <button onClick={() => handleDelete(memo.id)}>削除</button>
        </div>
      ))}
    </div>
  );
}

export default App;
```

---

## 4️⃣ Supabase のセットアップ

1. [https://supabase.com](https://supabase.com) にアクセスし、無料アカウントを作成
2. 新しいプロジェクトを作成
3. 左メニュー「Table Editor」→「New table」でテーブルを作成

### テーブル定義（`memos`）

| カラム名     | 型          | デフォルト | 備考           |
| ------------ | ----------- | ---------- | -------------- |
| `id`         | int8        | 自動生成   | Primary Key    |
| `title`      | text        | -          | メモのタイトル |
| `content`    | text        | `''`       | メモの本文     |
| `created_at` | timestamptz | `now()`    | 作成日時       |

または「SQL Editor」で以下を実行：

```sql
create table memos (
  id bigint generated always as identity primary key,
  title text not null,
  content text default '',
  created_at timestamptz default now()
);
```

4. 「Project Settings」→「API」から以下をコピーし、`server/.env` に貼り付け：
   - **Project URL** → `SUPABASE_URL`
   - **anon public key** → `SUPABASE_KEY`

---

## 5️⃣ 動作確認（ローカル）

ターミナルを2つ開いて、それぞれ実行します。

**ターミナル①（バックエンド）：**

```bash
cd memo-app/server
npm run dev
# → http://localhost:3001 で起動
```

**ターミナル②（フロントエンド）：**

```bash
cd memo-app/client
npm run dev
# → http://localhost:5173 をブラウザで開く
```

---

## 6️⃣ GitHubへのプッシュ

```bash
cd memo-app
git init
echo "node_modules/" >> .gitignore
echo ".env" >> .gitignore
git add .
git commit -m "initial commit"
```

GitHubで新しいリポジトリを作成し：

```bash
git remote add origin https://github.com/YOUR_NAME/memo-app.git
git push -u origin main
```

---

## 7️⃣ Render へのデプロイ

[https://render.com](https://render.com) に登録し、GitHubと連携します。

### バックエンドのデプロイ

1. 「New +」→「Web Service」
2. リポジトリを選択
3. 以下を入力：
   - **Root Directory**: `server`
   - **Build Command**: `npm install`
   - **Start Command**: `node index.js`
4. 「Environment Variables」に `.env` の内容を入力
5. 「Create Web Service」でデプロイ完了

### フロントエンドのデプロイ

1. 「New +」→「Static Site」
2. リポジトリを選択
3. 以下を入力：
   - **Root Directory**: `client`
   - **Build Command**: `npm run build`
   - **Publish Directory**: `dist`
4. 「Environment Variables」に以下を追加：
   - `VITE_API_URL` = Renderで発行されたバックエンドのURL

5. `client/src/App.jsx` の `API` 変数を更新：

```jsx
const API = import.meta.env.VITE_API_URL + "/api";
```

---

## ✅ 完成チェックリスト

- [ ] Node.js インストール済み
- [ ] `server/.env` に Supabase の情報を入力済み
- [ ] Supabase に `memos` テーブルを作成済み
- [ ] ローカルでメモの追加・編集・削除が動作する
- [ ] GitHub にプッシュ済み
- [ ] Render にデプロイ済み

---

## 🛠️ トラブルシューティング

| 症状                    | 原因・対処                                                 |
| ----------------------- | ---------------------------------------------------------- |
| `CORS error`            | `server/index.js` の `cors()` が正しく設定されているか確認 |
| `Cannot GET /api/memos` | サーバーが起動しているか確認（ターミナル①）                |
| Supabase接続エラー      | `.env` の URL と KEY が正しいか確認                        |
| Renderでスリープ        | 無料プランは15分無操作でスリープ。有料プランで解消         |

---

## 📚 次のステップ

- **認証の追加**: Supabase Auth を使ったログイン機能
- **タグ機能**: メモにカテゴリをつける
- **検索機能**: タイトル・本文でのメモ検索
- **マークダウン対応**: `react-markdown` を導入してリッチなメモ表示
